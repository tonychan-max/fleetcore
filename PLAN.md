# fleetcore プロジェクト全体計画（統合版 v2）

> **目的**：実務で担当している Windows ベースの多プロセス指令システムと同じアーキテクチャを、Linux + Qt6 + モダン C++ で再設計・再実装する。
> **性質**：**clean-room 再実装**。アーキテクチャ思想（汎用的な工学パターン）のみ流用し、会社のコード・電文番号・構造体・プロセス命名は一切持ち込まない。GitHub に公開できる成果物とする。
> **README で会社名・システム名・顧客名・電文番号は書かない。**「実務で類似システムを担当」まで。

---

## 0. このドキュメントについて

### v1 からの変更点

| 項目 | 内容 |
|---|---|
| **§3 を新設** | 「拡張のために今やっておくこと」。後から入れると作り直しになる項目だけを抽出 |
| **Phase 5 を新設** | マルチ端末シミュレーション |
| **Phase 6 を新設** | 地図・ナビゲーション連携 |
| **原則 1-7 / 1-8 を追加** | 外部依存の隔離、設定台帳 |
| **§6 を新設** | リポジトリ構成と ADR（設計判断の記録） |
| **§7 を新設** | 参考 OSS |
| **付録 A を新設** | 業界横展開マップ |

### 大原則（v1 から不変）

**骨格を先に通す。Phase 5・6 は Phase 1 が動いてから。**
§3 に挙げた項目だけは Phase 1 の時点で入れておく。それ以外は全部後回しでよい。

---

## 1. 全体アーキテクチャ

### 1-A. 基本形（Phase 1〜4）

```
maint_gui (Qt6)          保守画面：端末を選んでロックボタン
    │ TCP
    ▼
gateway   (1000)         対外窓口。電文を解析して内部メッセージへ
    │ msgq
    ▼
locator   (2000)         端末番号 → 端末索引を解決（共有メモリ参照）
    │ msgq
    ▼
termd     (3000+n)       端末ごとに1プロセス。TCP で端末へ
    │ TCP
    ▼
term_gui  (Qt6)          車載画面：ロック表示・解除認証
```

**プロセス索引に ID を埋め込む**（`3000 + 端末番号`）ことで、`termd` だけが複数プロセス並存できる。これは実務システムの最も美しい設計なので必ず再現する。

### 1-B. 拡張後の姿（Phase 5〜6 込み）

```
                    ┌──────────────────────────────┐
                    │  maint_gui (Qt6)             │
                    │   ├ 端末一覧・ロック操作      │
                    │   └ 地図表示（Phase 6）★      │
                    └──────────────┬───────────────┘
                                   │ TCP
                                   ▼
                    ┌──────────────────────────────┐
       ┌───────────▶│  gateway (1000)              │
       │            └──────────────┬───────────────┘
       │                           │ msgq
       │            ┌──────────────▼───────────────┐
       │            │  locator (2000)              │
       │            └──────────────┬───────────────┘
       │                           │ msgq
       │            ┌──────────────▼───────────────┐
       │            │  termd (3000+n) ×N           │
       │            └──────────────┬───────────────┘
       │                           │ TCP
       │         ┌─────────────────┼─────────────────┐
       │         ▼                 ▼                 ▼
       │   term_gui(実物)    term_sim ×N ★     term_sim ×N ★
       │   （Phase 2で        （Phase 5：       （異常注入用）
       │    多プロセス化）      ヘッドレス端末）
       │
       │            ┌──────────────────────────────┐
       └────────────┤  route_svc ★（Phase 6）      │
                    │   IRouteProvider             │
                    │    ├ MockProvider（既定）     │
                    │    ├ OsrmProvider            │
                    │    └ （任意）商用API Provider │
                    └──────────────────────────────┘
```

★ = v2 で追加

---

## 2. 設計原則

プロジェクト全体を貫く判断基準。迷ったらここに戻る。

### 1-1. 四層フレーム

| 層 | 内容 | 性質 |
|---|---|---|
| **電文の流れ** | バイト列の契約 | **契約**（両端で1バイトも違えない） |
| **業務駆動** | 解析 → 解決 → 変換 | 実装 |
| **フレームワーク** | プロセス構成・IPC の使い分け | 実装 |
| **言語/OS の道具** | 共有メモリ・セマフォ・スレッド | 実装 |

**契約は1つだけ**（`common/proto.h`、Phase 3 以降は生成物）。他はすべて各プロセスの自由。

### 1-2. 業務駆動は三動作の組み合わせ

どの電文も結局これしかやっていない。

| 動作 | fleetcore での担当 |
|---|---|
| **解析** | `gateway`：バイト列 → 構造体 |
| **解決** | `locator`：端末番号 → 端末索引 |
| **変換** | `termd`：内部形式 → 端末向け形式 |

### 1-3. 多対一なら漏斗、一対一なら不要

**ソケットに書き手が複数いるかどうか**で設計が決まる。

- `gateway` ← maint_gui は多対一になりうる → **送信キュー + 送信スレッドで直列化**
- `termd` → 端末は一対一（プロセスが端末ごと）→ **直接送信でよい**

TCP はバイトストリームなので、複数スレッドが同時に書くと電文が混ざる。キューはその直列化装置。

### 1-4. snapshot と delta を分ける

- **共有メモリ = snapshot**（今の状態、非破壊読み取り、何度でも読める）
- **メッセージ = delta**（起きた事、破壊的読み取り、1人だけが受け取る）

状態を変えたら「変えた」を別途通知する。共有メモリは通知機能を持たない。

### 1-5. claim-check（データは預け、キーだけ渡す）

同一マシン内はデータを共有領域に置いてキーだけ流す。跨マシンは全部送るしかない。
大きいファイルは別経路（実務では FTP）。

### 1-6. 落としていいもの・落としてはいけないもの

- **上り（状態通知）は落としてよい**：新しい値が来れば古い値に意味はない
- **下り（指令）は落としてはいけない**：store-and-forward + retry で必ず届ける

### 1-7. ★ 外部依存は必ずインタフェース越しに置く

Phase 6 で地図・経路探索サービスを繋ぐが、**具体的なサービス名がコードの奥に現れてはいけない**。

```cpp
class IRouteProvider {
public:
    virtual ~IRouteProvider() = default;
    virtual Result<Route> route(Coord from, Coord to) = 0;
};
```

- 既定実装は `MockRouteProvider`（直線を返すだけ）。**ネットワーク無しで CI が回ることが最優先**
- 実サービスは差し替え可能な一実装にすぎない

理由：外部サービスは**必ず**変わる。値上げ、規約変更、終了、レート制限。
実装をインタフェースの後ろに隔離しておけば、変わったときに壊れるのは1ファイルだけ。

> これは DDD で言う **腐敗防止層（anti-corruption layer）**。
> 実務のシステムが 15〜20 年保守できるのは、外部との境界が電文仕様書1枚に閉じているから。同じ構造を意識的に作る。

### 1-8. ★ 設定項目は台帳で一元管理する

実務で得た教訓の直輸入。設定フラグを機能ごとに場当たりで足していくと、
**「どのファイルの、どのキーを、いつ読んでいて、いつ反映されるか」が誰にも分からなくなる。**

fleetcore では最初から次を守る。

- 設定ファイルは **1つ**（`config/fleetcore.toml`）。プロセスごとにセクションを切る
- **`docs/CONFIG.md` に台帳を持つ**。列は次の通り

| キー | 既定値 | 読込プロセス | 読込タイミング | 反映条件 | 説明 |
|---|---|---|---|---|---|

- 「反映条件」列（即時 / 接続時 / プロセス再起動）を**必ず**埋める。空欄禁止
- 設定を追加する PR は、台帳の行追加を含まなければマージしない（自分ルール）

---

## 3. ★ 拡張のために「今」やっておくこと

**後から入れると作り直しになるものだけ**を挙げる。逆に言えば、ここに無いものは全部後回しでよい。

| # | 項目 | 入れる Step | やらなかった場合のコスト |
|---|---|---|---|
| 3-1 | ヘッダに `version` | Step 1 | プロトコル進化が一切できない。全端末同時更新が前提になる |
| 3-2 | ヘッダに `length` | Step 1 | 未知の電文を読み飛ばせない。新機能追加のたび全端末更新 |
| 3-3 | コード空間の区画割り | Step 1 | 後から番号が衝突し、既存端末と互換が切れる |
| 3-4 | 転送表のデータ駆動化 | Step 9 | 業務プロセス追加のたびに `router` を再コンパイル |
| 3-5 | 端末 capability | Step 3 | 新旧端末混在ができない。全台一斉更新が前提になる |
| 3-6 | 設定台帳 | Step 1 | §1-8 の通り |
| 3-7 | `IRouteProvider` 等の抽象化 | Phase 6 着手時 | 外部サービスの都合でコア設計が壊れる |

### 3-1 / 3-2　共通ヘッダ

```cpp
#pragma pack(push, 1)
struct Header {
    uint8_t  magic[2];   // 'F','C' — 同期・誤接続検出
    uint8_t  version;    // ★プロトコル版数。未知なら受信側が判断
    uint8_t  code;       // 電文種別
    uint16_t length;     // ★ヘッダ含む全長。未知電文の読み飛ばしに必須
    uint16_t seq;        // 連番。再送・重複検出
    uint64_t timestamp;  // 発生時刻（ミリ秒）
    uint32_t reserved;   // 将来用（0固定、static_assert でサイズ固定）
};
#pragma pack(pop)

static_assert(sizeof(Header) == 20, "共通ヘッダのサイズ");
static_assert(offsetof(Header, length) == 4, "length の位置");
```

> `length` があるだけで、**受信側は知らない電文を安全に捨てられる**。
> これが無いと「新しい電文を1つ足す」ために全端末を同時更新する羽目になる。
> 実務の長期保守システムで一番効くのはこの2バイト。

### 3-3　メッセージコード空間の区画割り

**今は空でよい。区画だけ決めておく。**

| 範囲 | 用途 | 方向 | 落としてよいか | 実装 Phase |
|---|---|---|---|---|
| `0x01`–`0x0F` | 接続管理（接続・切断・死活） | 双方向 | × | Phase 1 |
| `0x10`–`0x1F` | 制御指令（ロック・解除） | 下り | **×** | Phase 1 |
| `0x20`–`0x2F` | 状態通知（ロック完了・端末状態） | 上り | ○ | Phase 4 |
| `0x30`–`0x3F` | 位置・移動情報 | 上り | ○ | Phase 5 |
| `0x40`–`0x4F` | ナビゲーション（目的地設定・経路） | 双方向 | × | Phase 6 |
| `0x50`–`0x5F` | ファイル転送・大容量（claim-check） | 双方向 | × | 任意 |
| `0xF0`–`0xFF` | 試験・異常注入 | 双方向 | ○ | Phase 5 |

**「落としてよいか」を電文定義そのものに書く**（Phase 3 の YAML に `droppable: true/false` を持たせる）。
そうすると再送方針がコードに散らばらず、生成物が面倒を見る。

### 3-4　転送表はデータ駆動

`term_router` の転送規則は**ファイルから読む**（実務の `MessageForward.txt` 相当）。

```toml
# config/forward.toml
[[route]]
code_from = 0x10
code_to   = 0x1F
targets   = ["ui", "logger"]
```

業務プロセスを増やすとき、`router` のコードを触らない。これが Phase 5・6 を安く入れるための土台。

### 3-5　端末 capability

端末テーブル（共有メモリ）に `uint32_t capabilities` を持たせ、接続時に端末が申告する。

```cpp
constexpr uint32_t CAP_LOCK       = 1u << 0;
constexpr uint32_t CAP_POSITION   = 1u << 1;  // Phase 5
constexpr uint32_t CAP_NAVIGATION = 1u << 2;  // Phase 6
```

これで **新旧端末の混在**が成立する。位置報告できない古い端末に位置要求を投げない、といった判断が動的にできる。

> 実務では「機能フラグを ini で持つ」ことで似たことをやっているが、
> **端末側が申告する**ほうが正しい。サーバの設定と端末の実態がズレる事故が構造的に起きない。

---

## 4. ロードマップ

### Phase 1　骨格（CLI → GUI）

| Step | 内容 | 実務での対応 |
|---|---|---|
| 1 | プロトコル定義 + `static_assert` + CMake（**§3-1〜3-3 込み**） | 電文構造体定義 |
| 2 | CLI で骨幹貫通（gateway → locator → termd） | 3プロセス転送チェーン |
| 3 | 共有メモリ + セマフォ（端末テーブル、**capability 込み**） | 車両情報テーブル + 排他 |
| 4 | SQLite 認証（**パスワードはハッシュ保存**） | Access mdb + 平文比較 |
| 5 | `term_gui`（Qt6 ロック画面 + 解除認証） | 車載ロック画面 |
| 6 | `maint_gui`（Qt6 保守画面） | 保守画面 |
| 7 | signal / systemd（優雅終了・常駐・ログ） | プロセス監視 + 起動バッチ |
| 8 | gtest + GitHub Actions CI | （実務には無い） |

### Phase 2　端末内部の多プロセス化

実務の車載端末（全体制御 + ミドル連携 + 各画面）に相当する部分。

| Step | 内容 |
|---|---|
| 9 | `term_router`（中央交換機）+ **転送表ファイル（§3-4）** |
| 10 | `term_gui` を recv / router / ui の3プロセスに分割、**QLocalSocket** で接続 |
| 11 | `QSharedMemory` + 変更通知（snapshot / delta の分業を実装で確認） |

**QLocalSocket を選ぶ理由**：Linux では Unix domain socket、Windows では named pipe に自動で切り替わる。**同じコードが両 OS で動く**。依存ゼロ（Qt のみ）。D-Bus は dbus-daemon 常駐が前提になるため、組込み向けには重い。

> 比較対象として KDE Connect が D-Bus を選んでいる。あちらは `QDBusViewer` で接続を覗けるという恩恵を得ている。
> **こちらはその恩恵を捨てた**ので、代わりに Step 9 でログとトレースを厚めに作る。ADR に代償として明記する。

### Phase 3　契約の自動化（CAN / DBC 方式の模倣）

**このプロジェクトで最も語れる部分。**

実務で踏んだ「構造体にヘッダ定義が抜けていて、車番の読み取り位置が8バイトずれた」バグ。原因は**契約が人間向けの仕様書しかなく、機械が検証できなかった**こと。

自動車業界は同じ問題を DBC ファイル + コード生成で解決している（ECU が数百・サプライヤが数十社では人力照合が破綻するため）。それを最小構成で再現する。

| Step | 内容 |
|---|---|
| 12 | 電文定義ファイル（YAML）を設計する |
| 13 | 生成ツール（Python）：YAML → C++ 構造体 + `static_assert` + 組み立て/解析関数 |
| 14 | `proto.h` を生成物に置き換え、手書き定義を廃止 |
| 15 | ★ **第二のプロトコルを同じ生成器で通す**（業界非依存の証明） |

**定義ファイルのイメージ**

```yaml
protocol:
  name: fleetcore
  version: 1
  endian: big

messages:
  - name: LockRequest
    code: 0x10
    direction: down
    droppable: false        # ★再送方針を定義に持たせる（§3-3）
    since_version: 1        # ★どの版から存在するか
    fields:
      - { name: term_no, type: ascii, len: 4 }
```

**生成される C++**

```cpp
struct LockRequest {
    Header header;
    char   term_no[4];
};
static_assert(offsetof(LockRequest, term_no) == 20, "自動生成：位置検証");
static_assert(sizeof(LockRequest) == 24, "自動生成：サイズ検証");
```

**効果**：実務で1日かけて追いかけたバグが、**定義ファイルを直せば両端が同時に直る**構造になる。手で構造体を書く余地が消える。

#### Step 15 が重要な理由

「この生成器は業界非依存です」と README に書くのは簡単だが、**それは主張でしかない**。
fleetcore 自身のプロトコルに加えて、**もう1つ実在規格の簡略版**を同じ生成器で通せば、主張が証拠になる。

候補（どれか1つでよい）：

| 候補 | 実装量 | 効く相手 |
|---|---|---|
| **簡略 CAN フレーム群**（DBC 相当） | 小 | 車載・モビリティ業界。あなたの経歴と直結 |
| **MODBUS レジスタマップ** | 小 | 産業制御・FA。仕様が公開されていて短い |
| 簡略 HL7 v2 セグメント | 中 | 医療。可変長・区切り文字型なので生成器の一般性が上がる |

推奨は **簡略 CAN**。7年の自動車経験があるので中身の妥当性を自分で判断でき、
かつ「実務のバグ → 自動車業界の既存解 → 自作で再現」という物語が一本に繋がる。

### Phase 4　上り方向 + store-and-forward

- 入口と出口が入れ替わるだけで、形は同じ（`対外窓口 → 業務プロセス → 対外窓口`）
- **上りと下りで落とす方針が逆になる**（§1-6）ことを実装で示せる

| Step | 内容 |
|---|---|
| 16 | 上り電文（`0x20` 帯：ロック完了通知・端末状態） |
| 17 | `termd` に store-and-forward（端末オフライン時の保持と再送） |

### Phase 5　★ マルチ端末シミュレーション

**費用対効果が最も高い拡張。**
実務では実車が必要で絶対にできないことが、ここでは無料でできる。

| Step | 内容 |
|---|---|
| 18 | `term_sim`：ヘッドレス端末シミュレータ。1プロセスで N 台を模擬 |
| 19 | シナリオファイル（YAML）：台数・初期位置・移動経路・**異常注入** |
| 20 | 負荷試験：何台で破綻するか実測。`select` → `epoll` 移行を数字で正当化 |
| 21 | `maint_gui` に端末一覧・状態モニタ |

**シナリオファイルのイメージ**

```yaml
scenario:
  name: 50台・うち5台が異常
  terminals:
    - count: 45
      behavior: normal
      route: { from: [35.44, 139.63], to: [35.47, 139.62], speed_kmh: 40 }
    - count: 3
      behavior: flaky          # ランダム切断・再接続
    - count: 1
      behavior: slow_response  # 応答 10 秒遅延
    - count: 1
      behavior: malformed      # 長さ不正な電文を送る
```

**なぜこれが効くか**

1. **異常系テストが自動化できる。** 実務では「異常系は実機でどうにか再現する」しかない。ここでは異常が設定値になる
2. **`select` の限界を実測できる。** 記帳の「epoll と select の違い」が、机上の知識から**測定した事実**になる
3. **`store-and-forward` の検証手段になる。** Phase 4 の再送ロジックは、オフライン端末が居ないと試せない
4. **README の見栄えが決定的に変わる。** 地図上で20台が動いている GIF は、コードを読まない相手にも伝わる

> **Phase 6 より先にやる。** 位置情報の流れを先に作っておけば、Phase 6 は「それを地図に描くだけ」になる。

### Phase 6　★ 地図・ナビゲーション連携

| Step | 内容 |
|---|---|
| 22 | 位置情報電文（`0x30` 帯、上り、落としてよい系） |
| 23 | `maint_gui` に地図表示（Qt Location + OSM プラグイン） |
| 24 | `IRouteProvider` + `MockRouteProvider` / `OsrmRouteProvider` |
| 25 | `term_gui` に簡易ルート案内（目的地設定 → 経路表示 → 進捗） |

#### 技術選定の前提（要確認事項あり）

Qt6 の Qt Location について、着手前に必ず自分で最新ドキュメントを確認すること。
2026 年時点で把握している範囲：

- **地図表示は可能。** `MapView` 型がタップ・ドラッグ等の基本入力を扱う（Qt5 の `MapGestureArea` は廃止）
- **OSM プラグインは Qt6 でも利用可。** プラグインキーは `"osm"`。ただし既定のタイル提供元は予告なく変わりうると明記されているため、**本番用途では提供元を自分で指定し規約を確認する責任がある**
- **経路探索は `osm.routing.host` パラメータで自前サーバに向けられる。** ここに OSRM を立てれば経路が取れる
- ⚠️ **turn-by-turn ナビゲーションの実験的フレームワークは Qt6 で削除された。** 案内ロジック（次の交差点、リルート判定、到着判定）は**自作になる**

> 最後の点は**むしろ好都合**。実務では Mapple が全部やってくれていた部分を、自分で書ける。
> 「地図ライブラリを繋いだ」より「案内ロジックを書いた」ほうが、語れる中身がある。

#### Google Maps API について

**結論：インタフェースは用意する。既定にはしない。**

| 観点 | 判断 |
|---|---|
| 技術的可否 | 可能（REST + JSON）。`IRouteProvider` の一実装として問題なく入る |
| 費用 | 従量課金。課金アカウント必須 |
| API キー | **公開リポジトリに置けない。** 環境変数・シークレット管理が前提 |
| 利用規約 | 表示方法・キャッシュ・他社地図との併用に制約がある。**着手時点の規約を必ず自分で確認すること** |
| CI | ネットワークと課金に依存するテストは CI を不安定にする |

**したがって設計は次の通り。**

```
IRouteProvider（抽象）
  ├─ MockRouteProvider    ← 既定。直線+固定速度。CI はこれだけを使う
  ├─ OsrmRouteProvider    ← 実地図。self-host または公開デモサーバ
  └─ (任意) 商用API Provider ← 環境変数にキーがあるときだけ有効化
```

**この構造そのものが README のネタになる。**
「外部サービスに依存しない設計にした理由」を ADR に書けば、
外部依存・可用性・コストを設計判断として扱えることの証明になる。単に地図が出ているより価値が高い。

#### 位置情報の扱い（§1-6 の実演）

位置情報は**落としてよい**電文の典型。

- 1秒ごとに全端末が位置を送る → 50台なら 50 msg/s
- 1つ落ちても、次の値が来れば古い値に意味は無い
- **したがって再送しない。キューが詰まったら古いものから捨てる**

一方でナビの目的地設定（`0x40` 帯）は**落としてはいけない**。
**同じ地図機能の中に、逆の方針を持つ電文が同居する。** これは §1-6 を説明する最良の実例になる。

---

## 5. Step 1 の要点（着手時の参照用）

```
fleetcore/
├── CMakeLists.txt
├── common/
│   ├── proto.h            ← 唯一の契約（Phase 3 で生成物に置換）
│   └── layout_dump.cpp    ← レイアウト確認用
└── build/
```

**必ずやる実験**：`LockRequest` から `Header header;` を消して `make` する。

```
error: static assertion failed: 端末番号は共通ヘッダの直後から始まる
```

**実務で1日かかったバグが、1秒でコンパイルエラーになる。** これがプロジェクト全体の主題であり、README に載せるスクリーンショット。

### 現代 C++ の書き方（実務との対比）

| 実務 | fleetcore | 差 |
|---|---|---|
| `#define D_CODE 0x69` | `constexpr uint8_t CODE_LOCK = 0x10;` | 型・スコープあり、**全角スペースが混入したらコンパイルエラー** |
| `UCHAR` / `ULLONG` | `uint8_t` / `uint64_t` | ビット幅が名前にある、全プラットフォーム共通 |
| （対応物なし） | `static_assert` | コンパイル時検証、実行時コストゼロ |
| `char[]` で padding 回避 | `#pragma pack(1)` + `static_assert` | 明示 + 検証 |

---

## 6. リポジトリ構成と ADR

### 6-A. 構成

```
fleetcore/
├── README.md
├── CMakeLists.txt
├── docs/
│   ├── ARCHITECTURE.md        ← 全体構造・データフロー（この計画書の要約）
│   ├── PROTOCOL.md            ← 電文一覧（Phase 3 以降は生成）
│   ├── CONFIG.md              ← ★設定台帳（§1-8）
│   └── decisions/             ← ADR
│       ├── 0001-qlocalsocket-over-dbus.md
│       ├── 0002-sqlite-hashed-password.md
│       ├── 0003-static-assert-layout-check.md
│       ├── 0004-codegen-over-handwritten-proto.md
│       ├── 0005-route-provider-abstraction.md
│       └── 0006-drop-policy-per-message.md
├── common/
├── gateway/  locator/  termd/
├── term_gui/  maint_gui/
├── term_sim/                  ← Phase 5
├── tools/codegen/             ← Phase 3
├── config/
│   ├── fleetcore.toml
│   ├── forward.toml
│   └── scenarios/
└── tests/
```

### 6-B. ADR について

**ADR（Architecture Decision Record）= 設計判断1件につき1ファイル。**

```markdown
# 0001. プロセス間通信に QLocalSocket を使う

## 状態
採用（2026-08-xx）

## 背景
端末内部を複数プロセスに分割する（Phase 2）。プロセス間通信の方式を決める必要がある。

## 選択肢
1. D-Bus — Linux デスクトップの標準。KDE Connect が採用
2. QLocalSocket — Qt 標準。Unix domain socket / named pipe を抽象化
3. 生の Unix domain socket
4. 共有メモリ + セマフォのみ

## 決定
QLocalSocket。

## 理由
- 依存が Qt だけで完結する。dbus-daemon の常駐を前提にしない
- 同じコードが Linux / Windows 両方で動く
- 組込み・車載向けを想定した場合、D-Bus は重い

## 代償
- D-Bus が持つ内省ツール（QDBusViewer 等）が使えない
  → 対策：router 層に電文トレースログを実装する（Step 9）
- サービス発見の仕組みが無い。接続先パスを設定で持つ必要がある
```

**なぜやるか**

1. **次に開いた対話・次の月の自分に、正確な文脈を渡せる。** 記憶に頼らない
2. **これ自体が成果物。** ADR ディレクトリがある repo は、設計をした人間の repo に見える
3. **実務に持ち帰れる。** 筑後で「なぜこの ini は初期化で戻らないのか」を書き残せていない問題の解が、まさにこれ

---

## 7. 参考 OSS

**使い方の原則：自分で書いてから読む。** 先に読むと写経になり、学習効果が消える。

| 名前 | ライセンス | 対応 Phase | 何を見るか |
|---|---|---|---|
| **cantools** | MIT | Phase 3 | DBC → C 構造体 + pack/unpack 生成。生成器の実装そのもの |
| **comFramework** | 要確認 | Phase 3 | cantools と設計思想が逆（静的確保・上限保証）。**対比が価値** |
| **Qt Remote Objects** | LGPL / 商用 | Phase 2・3 | `.rep` DSL → `repc` で生成。「契約先行」の Qt 公式解 |
| **KDE Connect** | **GPL** | Phase 2・5 | daemon + 複数 UI + プラグイン構成。`lockdevice` プラグインあり |
| **OSRM** | BSD-2 | Phase 6 | 自前経路探索サーバ |
| **xsecurelock / swaylock** | 要確認 | Phase 1 Step 5 | 「本当にロックする」ための脅威モデル・多プロセス分離 |

### ⚠️ ライセンス上の注意

- **KDE Connect は GPL。** 構造と思想を読むのは自由だが、**コード片を持ち込んではいけない**。読んだら閉じて、自分の言葉で ADR に書いてから実装する
- **Qt 本体は LGPL または商用。** 動的リンクで使う限り問題ないが、LGPL の差し替え可能性要件は理解しておく
- cantools は MIT なので安心して読める。**まず触るならここ**

### 特に効く2つ

**cantools**：`pip install cantools` → `cantools generate_c_source xxx.dbc`。
30分で生成物が見られる。7年の自動車経験があるので DBC は新規知識ではない。**Phase 3 の構想を最速で具体化できる入口。**

**Qt Remote Objects**：`.rep` の文法を、**自分の YAML を設計したあとに**一語ずつ見る。
「この関键字は何の問題を解いているのか、自分は漏らしていないか」を問う。これが対比学習。

---

## 8. README に書くこと

- アーキテクチャ図（gateway → locator → termd → term_gui / term_sim）
- **なぜこの設計にしたか**（claim-check、プロセス索引に ID を埋める理由、多対一だけ漏斗が要る理由）
- **`static_assert` によるレイアウト検証** ＋ わざと壊してコンパイルを落とすスクリーンショット
- **Phase 3 のコード生成**：なぜ人力の仕様書では不十分か。**第二プロトコルで業界非依存性を実証**
- ★ **マルチ端末シミュレーション**：50台稼働の GIF、異常注入シナリオ、負荷測定グラフ
- ★ **外部サービスを既定にしない設計**：`IRouteProvider` と、そう決めた理由
- Windows / Linux 対応表
- テスト・CI
- `docs/decisions/` への導線

**面接で自然に引き出せる話題**

| 話題 | 裏付け |
|---|---|
| 共有メモリとメッセージキューの使い分け | 実装 + ADR |
| バイナリプロトコルのレイアウトずれをどう防ぐか | **実体験あり** + 生成器 |
| 部分失敗・オフライン端末をどう扱うか | store-and-forward の実務経験 + シミュレータで実証 |
| ★ 外部 API 依存をどう設計に閉じ込めるか | `IRouteProvider` + ADR-0005 |
| ★ 実機が用意できない環境でどうテストするか | Phase 5 全体。**実務の制約から生まれた発想** |
| ★ 長期保守を前提としたプロトコル設計 | `version` / `length` / capability（§3） |

---

## 9. 記帳（骨格が通ってから展開する支線）

- RAII と `std::unique_ptr`（Step 2 で自然に出会う）
- `enum class` と普通の enum の違い
- `std::optional`（認証結果の表現で欲しくなる）
- ムーブセマンティクス / `std::move`
- range-based for / `auto`
- `std::chrono`（タイムアウト処理）
- **epoll と select の違い**（実務は select。**Phase 5 Step 20 で実測して移行判断する**）
- gdb / strace（実務では debug 環境が無いため、この差を体感する価値が大きい）
- `std::variant` / `std::visit`（電文の型付き分岐。Phase 3 の生成物と相性がよい）

---

## 10. 進め方

**骨格を先に通す。細部は取り付け点ができてから。**

各 Step で「なぜこう書くか・実務のやり方とどう違うか」は都度説明するが、支線は追わず記帳して先へ進む。
「ロック画面が初めて実際にロックされる瞬間」を早く手に入れることを優先する。

### 優先順位（迷ったとき）

```
Phase 1（骨格）
    ↓  ここまでは何があっても通す
Phase 3（生成器）  ← 語れる中身の核。Phase 2 より先でもよい
    ↓
Phase 5（シミュレータ）  ← 見栄えと検証手段。費用対効果が高い
    ↓
Phase 2（端末内部分割）／ Phase 4（上り）
    ↓
Phase 6（地図）  ← 最後でよい。外部依存が入るので一番不安定
```

> **Phase 6 を最初にやりたくなるのを我慢する。** 地図は目に見えるので魅力的だが、
> 外部サービス・API キー・規約という**プロジェクトの本題と無関係な問題**を大量に持ち込む。
> 骨格と生成器が先。

---

## 付録 A　業界横展開マップ

**移植可能な資産は「MFC が書ける」ではなく「契約駆動の分散システムを読める」。**
四層フレーム（§1-1）の上3層はそのまま効き、変わるのは最下層（言語・OS・道具）だけ。

### A-1. 業界一覧

| 業界 | 代表的な規格・プロトコル | fleetcore のどこがそのまま効くか | あなたの既存資産 | 距離 |
|---|---|---|---|---|
| **車載・モビリティ** | CAN / DBC、SOME/IP、UDS、FMS | Phase 3 生成器がほぼそのまま。固定長・エンディアン・信号ビット配置 | **自動車部品開発7年**＋現職の車載端末 | ★★★ 最短 |
| **物流・配車** | 独自 TCP 電文、動態管理、EDI | 構造が同型（指令→車両→状態上り）。Phase 5 のマルチ車両がそのまま業務 | 現職とほぼ同じ業務構造 | ★★★ |
| **産業制御・FA** | MODBUS、OPC UA、EtherCAT、PLC | レジスタマップ＝固定長契約。決定性・長期保守 | 自動車の生産技術側と接点 | ★★☆ |
| **エネルギー・インフラ** | IEC 61850、ECHONET Lite、B ルート | 多拠点・常時接続・状態収集。上り落としてよい系の典型 | 直接の経験は無い | ★★☆ |
| **金融** | ISO 8583、FIX、全銀手順 | **固定長＋ビットマップの契約設計**が非常に近い。24/7、長期保守 | 業務ドメイン知識ゼロが壁 | ★☆☆ |
| **医療** | HL7 v2、DICOM、SS-MIX2 | 生命安全・長期保守・規格駆動。区切り文字型なので生成器の一般化ネタ | 資格・規制の壁が高い | ★☆☆ |
| **鉄道・航空** | 運行管理、信号系 | 安全要求が最も高い。多重化・フェイルセーフ | 参入に認証・経験年数の壁 | ★☆☆ |
| **公共安全（現職）** | 消防・警察・救急指令 | — | 現在地 | — |

### A-2. 現実的な狙い方

**第一候補：車載・モビリティ**

理由は3つ重なるから。

1. 自動車部品開発7年 —— ECU・DFMEA・公差設計・設計変更管理が**すでに手の内にある**
2. 現職の車載端末システム —— 車両側ソフトの実務経験
3. fleetcore の Phase 3 —— CAN/DBC 方式のコード生成を**自分で実装した**

**この3つが揃っている人は多くない。** 「自動車から IT に転職した人」でも「CAN の契約設計を自分で書いた人」はほとんど居ない。

Phase 3 Step 15 で簡略 CAN を第二プロトコルに選ぶと、この3点が1つの成果物に集約される。**やるならこれ。**

**第二候補：物流・配車**

現職の業務構造と最も近い。「指令を車両に配り、状態を集める」は消防も配送も同型。
Phase 5 のマルチ端末シミュレーションが、そのまま「配車シミュレーション」として読める。
公共安全ドメインの知識がほぼ 100% 通用する点が強い。

**構造は近いが壁があるもの：金融・医療**

技術的には最も近い（固定長バイナリ契約、24/7、15年保守）が、
**業務ドメイン知識が採用要件の中心**になるため、技術力だけでは入りにくい。
「構造が同じだと分かっている」ことは面接での説得力になるが、これ単独で狙うのは非効率。

### A-3. fleetcore を「横展開の証拠」にする最小の一手

言葉で「業界非依存です」と言っても価値はほぼゼロ。**Step 15（第二プロトコル）が唯一の証明手段。**

```
tools/codegen/
├── schema/
│   ├── fleetcore.yaml     ← 自分のプロトコル
│   └── sample_can.yaml   ← ★簡略 CAN フレーム群
└── generate.py           ← 同じ生成器が両方を処理する
```

これだけで README にこう書ける。

> この生成器は fleetcore 独自プロトコルと、簡略化した CAN フレーム定義の両方を、
> **同一のコードパスで処理する。** バイト列の契約という問題は業界を問わず同じ形をしており、
> 生成器はその形にだけ依存している。

**主張が検証可能になる。** これが付録 A 全体の実装上の結論。

---

*v1 作成：遠隔ロック機能の実装完了後、Linux/Qt6 への再設計プロジェクトとして。*
*v2 統合：拡張設計（シミュレーション・地図連携）、ADR 運用、業界横展開マップを追加。*
